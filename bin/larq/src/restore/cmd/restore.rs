use crate::cli::RestoreOpts;
use arq::{format_uuid, Repository};

pub async fn restore(repo: &Repository, args: RestoreOpts) -> Result<(), ()> {
    // fetch computer info
    // let computer_id = format_uuid(&args.computer);
    // let decrypter =
    // let folders = repo.list_folders(computer_id: &str, decrypter: &dyn ObjectDecrypter)
    // // enumerate / select folder
    // // selct commit (master)
    Ok(())
}

//fn list_folders(repo: &Repository, )
